
<%= form_with model: @tag, url: new_tag_path(user_id: @user), method: :post, class: 'form' do |form| %>
  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_area :name %>
  </div>
  
    <%= form.submit %>
  </div>

<% end %>